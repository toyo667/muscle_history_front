import { Box } from "@mui/material";
import { MasterData } from "../../../hooks/useMasters";

interface Props {
  masterData: MasterData;
}

export const AddWorkout: React.FC<Props> = ({ masterData }) => {
  return <Box>add workout</Box>;
};
